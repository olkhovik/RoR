<% form_params = @comment.new_record? ? [@post, @comment] : @comment%>
<%= form_for(form_params) do |f| %>
  <%= render 'common/errors', object: @comment %>

  <div class="field">

    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit "Publish Comment"%>
  </div>
<% end %>
