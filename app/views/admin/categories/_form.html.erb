<%= form_for([:admin, @category]) do |f| %>
    <%= render 'common/errors', object: @category %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

